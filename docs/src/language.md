# The words this project uses

One name per thing, and the names it deliberately avoids. Worth reading
before the guide: the documents, the code and the error messages all use
these words in exactly this sense, so "box", "role" and "grant" are never
loose synonyms for something else.

Rendered unchanged from
[`CONTEXT.md`](https://github.com/naborka/wormhole/blob/dev/CONTEXT.md)
at the repository root.

---

{{#include ../../CONTEXT.md}}
